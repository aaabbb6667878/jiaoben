-- ==================== 透视（Highlight） ====================
local FillColor = Color3.fromRGB(175, 25, 255)
local DepthMode = "AlwaysOnTop"
local FillTransparency = 0.5
local OutlineColor = Color3.fromRGB(255, 255, 255)
local OutlineTransparency = 0

local CoreGui = game:FindService("CoreGui")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Cam = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer
local connections = {}

local Storage = Instance.new("Folder")
Storage.Parent = CoreGui
Storage.Name = "Highlight_Storage"

local masterEnabled = true

function Highlight(Player)
    if not masterEnabled then return end
    local old = Storage:FindFirstChild(Player.Name)
    if old then old:Destroy() end
    if connections[Player] then connections[Player]:Disconnect() end

    local hl = Instance.new("Highlight")
    hl.Name = Player.Name
    hl.FillColor = FillColor
    hl.DepthMode = DepthMode
    hl.FillTransparency = FillTransparency
    hl.OutlineColor = OutlineColor
    hl.OutlineTransparency = 0
    hl.Parent = Storage

    local char = Player.Character
    if char then hl.Adornee = char end

    connections[Player] = Player.CharacterAdded:Connect(function(c)
        if masterEnabled and hl.Parent then hl.Adornee = c end
    end)
end

function clearAllHighlights()
    for _, child in pairs(Storage:GetChildren()) do
        child:Destroy()
    end
end

function restoreAllHighlights()
    for _, p in pairs(Players:GetPlayers()) do
        Highlight(p)
    end
end

Players.PlayerAdded:Connect(function(p)
    if masterEnabled then Highlight(p) end
end)
for _, p in pairs(Players:GetPlayers()) do Highlight(p) end

Players.PlayerRemoving:Connect(function(Player)
    local name = Player.Name
    if Storage:FindFirstChild(name) then Storage[name]:Destroy() end
    if connections[Player] then connections[Player]:Disconnect() connections[Player] = nil end
end)

-- ==================== 绿色自瞄（FOV=150） ====================
local fov = 150

local FOVring = Drawing.new("Circle")
FOVring.Visible = true
FOVring.Thickness = 2
FOVring.Color = Color3.fromRGB(0, 255, 0)
FOVring.Filled = false
FOVring.Radius = fov
FOVring.Position = Cam.ViewportSize / 2

local function lookAt(target)
    local lookVector = (target - Cam.CFrame.Position).unit
    local newCFrame = CFrame.new(Cam.CFrame.Position, Cam.CFrame.Position + lookVector)
    Cam.CFrame = newCFrame
end

local function getClosestPlayerInFOV(trg_part)
    local nearest = nil
    local last = math.huge
    local center = Cam.ViewportSize / 2
    for _, Player in ipairs(Players:GetPlayers()) do
        if Player ~= LocalPlayer then
            local part = Player.Character and Player.Character:FindFirstChild(trg_part)
            if part then
                local ePos, onScreen = Cam:WorldToViewportPoint(part.Position)
                local dist = (Vector2.new(ePos.X, ePos.Y) - center).Magnitude
                if dist < last and onScreen and dist < fov then
                    last = dist
                    nearest = Player
                end
            end
        end
    end
    return nearest
end

RunService.RenderStepped:Connect(function()
    FOVring.Position = Cam.ViewportSize / 2
    FOVring.Visible = masterEnabled
    if not masterEnabled then return end
    local closest = getClosestPlayerInFOV("Head")
    if closest and closest.Character and closest.Character:FindFirstChild("Head") then
        lookAt(closest.Character.Head.Position)
    end
end)

-- ==================== 无后坐力 ====================
RunService.Stepped:Connect(function()
    if not masterEnabled then return end
    local char = LocalPlayer.Character
    if not char then return end
    for _, tool in pairs(char:GetChildren()) do
        if tool:IsA("Tool") then
            local handle = tool:FindFirstChild("Handle")
            if handle then
                handle.Velocity = Vector3.new(0, 0, 0)
                handle.RotVelocity = Vector3.new(0, 0, 0)
            end
            local fireEvent = tool:FindFirstChild("FireEvent")
            if fireEvent then fireEvent:Destroy() end
        end
    end
end)
-- ==================== 第三人称切换 ====================
local UserInputService = game:GetService("UserInputService")
local thirdPersonEnabled = false
local defaultMaxZoom = Cam.CameraMaxZoomDistance

local tpBtn = Drawing.new("Square")
tpBtn.Filled = true
tpBtn.Size = Vector2.new(50, 50)
tpBtn.Position = Vector2.new(20, 160)
tpBtn.Color = Color3.new(0.3, 0.3, 0.8)
tpBtn.Visible = true
tpBtn.ZIndex = 999

local tpText = Drawing.new("Text")
tpText.Text = "3P"
tpText.Size = 16
tpText.Center = true
tpText.Position = Vector2.new(45, 185)
tpText.Color = Color3.new(1, 1, 1)
tpText.Visible = true
tpText.ZIndex = 1000

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.UserInputType == Enum.UserInputType.MouseButton1 or
       input.UserInputType == Enum.UserInputType.Touch then
        local pos = UserInputService:GetMouseLocation()
        if pos.X >= 20 and pos.X <= 70 and pos.Y >= 160 and pos.Y <= 210 then
            thirdPersonEnabled = not thirdPersonEnabled
            if thirdPersonEnabled then
                tpBtn.Color = Color3.new(0.2, 0.2, 0.9)
                tpText.Text = "3P"
                Cam.CameraType = Enum.CameraType.Custom
                Cam.CameraMaxZoomDistance = 20
                LocalPlayer.CameraMaxZoomDistance = 20
            else
                tpBtn.Color = Color3.new(0.3, 0.3, 0.8)
                tpText.Text = "3P"
                Cam.CameraType = Enum.CameraType.Custom
                Cam.CameraMaxZoomDistance = defaultMaxZoom
                LocalPlayer.CameraMaxZoomDistance = defaultMaxZoom
            end
        end
    end
end)

-- ==================== 总开关 ====================
local toggleBtn = Drawing.new("Square")
toggleBtn.Filled = true
toggleBtn.Size = Vector2.new(40, 40)
toggleBtn.Position = Vector2.new(20, 100)
toggleBtn.Color = Color3.new(0.2, 0.8, 0.2)
toggleBtn.Visible = true
toggleBtn.ZIndex = 999

local btnText = Drawing.new("Text")
btnText.Text = "ON"
btnText.Size = 16
btnText.Center = true
btnText.Position = Vector2.new(40, 120)
btnText.Color = Color3.new(1, 1, 1)
btnText.Visible = true
btnText.ZIndex = 1000

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.UserInputType == Enum.UserInputType.MouseButton1 or
       input.UserInputType == Enum.UserInputType.Touch then
        local pos = UserInputService:GetMouseLocation()
        if pos.X >= 20 and pos.X <= 60 and pos.Y >= 100 and pos.Y <= 140 then
            masterEnabled = not masterEnabled
            if masterEnabled then
                toggleBtn.Color = Color3.new(0.2, 0.8, 0.2)
                btnText.Text = "ON"
                restoreAllHighlights()
            else
                toggleBtn.Color = Color3.new(0.8, 0.2, 0.2)
                btnText.Text = "OFF"
                clearAllHighlights()
                FOVring.Visible = false
            end
        end
    end
end)
